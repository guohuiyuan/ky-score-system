{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "ky-score-system",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/main.go",
            "showLog": true,
            "cwd": "${workspaceFolder}"
        }
    ]
}